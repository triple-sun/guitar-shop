export enum ErrorType {
  NotFound = 'NotFound',
  Exists = 'Exists',
  Invalid = 'Invalid',
  Permission = 'Permission',
  Email = 'Email',
  Length = 'Length',
  ENV = 'ENV',
  Common = 'Common',
}
